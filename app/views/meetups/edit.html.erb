<h1>Add a Comment!</h1>
<%= form_for @meetup,:url => edit_user_meetup_path(@user) do |f| %>
  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
    <%= f.submit 'add this comment' %>
  </div>
<% end %>
